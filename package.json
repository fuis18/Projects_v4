{
  "dependencies": {
    "concurrently": "10.0.5"
  },
  "scripts": {
    "start": "concurrently \"cd frontend && pnpm start\" \"cd backend && pnpm start\"",
    "dev": "concurrently \"cd frontend && pnpm dev\" \"cd backend && pnpm dev\"",
    "build": "concurrently \"cd frontend && pnpm build\""
  }
}
